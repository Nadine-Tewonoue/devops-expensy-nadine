apiVersion: v1
kind: Service
metadata:
  name: {{ include "expensy.fullname" . }}
  labels:
    {{- include "expensy.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "expensy.selectorLabels" . | nindent 4 }}
